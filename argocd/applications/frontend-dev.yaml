# Argo CD Application for the DevoRecruiter frontend in the dev environment.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: devorecruiter-frontend-dev
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/farah-ben-harb/devorecruiter-gitops.git
    targetRevision: main
    path: charts/frontend
    helm:
      valueFiles:
        - ../../environments/dev/frontend-values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: devorecruiter-dev

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
