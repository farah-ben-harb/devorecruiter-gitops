# Argo CD Application for the DevoRecruiter BFF API in the dev environment.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: devorecruiter-api-bff-dev
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/farah-ben-harb/devorecruiter-gitops.git
    targetRevision: main
    path: charts/api-bff
    helm:
      valueFiles:
        - ../../environments/dev/api-bff-values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: devorecruiter-dev

  syncPolicy:
    syncOptions:
      - CreateNamespace=true