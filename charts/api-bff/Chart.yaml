# Helm chart API version. v2 is used by Helm 3 application charts.
apiVersion: v2

# Chart name. This becomes part of default Kubernetes resource names.
name: devorecruiter-api-bff

# Human-readable purpose of this chart.
description: Helm chart for the DevoRecruiter NestJS BFF API

# Application charts deploy runnable software.
type: application

# Chart package version. Increase this when chart templates or defaults change.
version: 0.1.1

# Application version. This matches the BFF Docker image tag built from the source commit.
appVersion: "b8ad515917c34131239c5b3eac3cf6eb7f64b80c"
