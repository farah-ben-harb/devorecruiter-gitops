# ConfigMap stores non-secret runtime settings for the n8n container.
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "devorecruiter-n8n.fullname" . }}
  labels:
    {{- include "devorecruiter-n8n.labels" . | nindent 4 }}
data:
  # Render every key under values.yaml env as a container environment variable.
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
